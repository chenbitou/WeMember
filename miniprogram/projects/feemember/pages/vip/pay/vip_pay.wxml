<view wx:if="{{!isLoad}}" class="margin-top load loading text-l text-grey"></view>
<view class="main" wx:if="{{isLoad&&isLogin}}">
	<view class="text-l text-project">会员有效期：{{startTime}} ~ {{endTime}}</view>

	<view bindtap="bindPayTap" class="btn-base btn-project shadow text-center btn-bg">马上缴费</view>


</view>